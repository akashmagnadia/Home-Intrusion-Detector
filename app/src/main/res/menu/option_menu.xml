<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <item
        android:id="@+id/settings"
        android:title="@string/setting"
        app:showAsAction="never"
        android:visible="false"/>

    <item
        android:id="@+id/testEmail"
        android:title="@string/send_test_email"
        app:showAsAction="never"
        android:visible="false"/>

    <item
        android:id="@+id/activate"
        android:title="Activate"
        app:showAsAction="never"
        android:visible="true">

        <menu>
            <item
                android:id="@+id/instantOn"
                android:title="Activate Monitoring now"
                app:showAsAction="never"
                android:visible="true"/>

            <item
                android:id="@+id/delay30secondOn"
                android:title="Activate Monitoring in 30 seconds"
                app:showAsAction="never"
                android:visible="true"/>

            <item
                android:id="@+id/delay1minuteOn"
                android:title="Activate Monitoring in 1 minute"
                app:showAsAction="never"
                android:visible="true"/>

            <item
                android:id="@+id/delay2minuteOn"
                android:title="Activate Monitoring in 2 minutes"
                app:showAsAction="never"
                android:visible="true"/>
        </menu>

    </item>

</menu>